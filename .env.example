# Database Configuration
DB_SERVER=localhost
DB_USERNAME=root
DB_PASSWORD=
DB_NAME=wackys

# For production deployment, set these variables in your hosting provider's dashboard
# (e.g., Heroku Config Vars, Vercel Environment Variables, or your cPanel environment setup)
